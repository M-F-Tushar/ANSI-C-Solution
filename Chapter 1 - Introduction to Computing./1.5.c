/*Write an algorithm and draw a flowchart to find the sum of the
digits of a 3-digit number.

1.5 Algorithm and Flowchart to Find the Sum of the Digits of a 3-Digit Number
Algorithm:
Start
Input a three-digit number N.
Extract digits:
D1 = N // 100 (Hundreds place)
D2 = (N // 10) % 10 (Tens place)
D3 = N % 10 (Units place)
Compute sum S = D1 + D2 + D3.
Display S.
Stop.
Flowchart:
Start → Input N → Extract D1, D2, D3 → Compute S = D1 + D2 + D3 → Display S → Stop.
*/
