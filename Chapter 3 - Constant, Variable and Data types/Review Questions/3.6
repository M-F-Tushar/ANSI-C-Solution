3.6 What is the difference between #include <file> and #include "file"?
#include <file> searches for the file in standard system directories.
#include "file" searches for the file in the current directory first, and if not found, then in the standard directories.
