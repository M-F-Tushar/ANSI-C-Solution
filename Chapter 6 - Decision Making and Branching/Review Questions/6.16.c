
Problem 6.16
Code:
c
char ch = 'a';
switch (ch) {
    case 'a':
        printf("A");
        break;
    case 'b':
        printf("B");
        break;
    default:
        printf("C");
}
Step-by-Step Execution:
Character
𝑐h=′𝑎′.

The switch statement evaluates 𝑐h:

Case 'a' matches, so "A" is printed, and the break statement stops further execution.

Output:
A
