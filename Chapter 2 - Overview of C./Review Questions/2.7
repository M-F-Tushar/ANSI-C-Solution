**2.7 Why and when do we use the `#include` directive?**
**Answer:** The `#include` directive is used to include header files that contain declarations of functions and macros. It is used when a program needs to use standard library functions like `printf`, `scanf`, etc.
