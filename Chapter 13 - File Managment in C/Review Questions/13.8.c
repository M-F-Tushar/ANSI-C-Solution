✅ 13.8 What are the common uses of rewind() and ftell() functions?
rewind(fp) – Moves the file pointer to the beginning of the file.

ftell(fp) – Returns the current position of the file pointer (as a long int).


