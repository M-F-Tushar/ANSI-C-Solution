✅ 13.4 What is the significance of EOF?
EOF (End Of File) is a special constant (usually -1) that signals the end of a file stream.

It’s returned by functions like getc() or scanf() when there is no more data to read.
