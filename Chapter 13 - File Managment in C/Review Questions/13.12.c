✅ 13.12 What does this statement do?
c
Copy code
while ((c = getchar()) != EOF)
    putc(c, fl);
Reads characters from input (keyboard) using getchar()

Writes them to a file (fl) using putc()

Stops at EOF (Ctrl+Z in Windows, Ctrl+D in Linux)


