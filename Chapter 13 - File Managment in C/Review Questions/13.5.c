
✅ 13.5 Why is it necessary to close a file during execution if files are auto-closed after program ends?
To free up system resources (memory, file handles).

To flush the buffer (ensure all data is written).

Prevent data corruption or data loss in case of crashes.
