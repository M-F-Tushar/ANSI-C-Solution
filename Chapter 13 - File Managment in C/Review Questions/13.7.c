✅ 13.7 How does append mode differ from write mode?
Write mode ("w"):

Creates a new file or deletes the contents of an existing one.

Append mode ("a"):

Creates a file if it doesn’t exist, adds new data at the end if it does.
