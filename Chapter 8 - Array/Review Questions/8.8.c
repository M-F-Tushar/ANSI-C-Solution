
8.8 What happens when an array with a specified size is assigned
(a) with values fewer than the specified size;
👉 Remaining values are initialized to zero (if the array is static) or garbage values (if the array is automatic).

(b) with values more than the specified size.
👉 It results in a compilation error or buffer overflow if it happens at runtime.

