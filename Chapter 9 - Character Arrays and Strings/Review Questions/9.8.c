9.8 What will be the output of the following statement?

printf("%d", strcmp("push", "pull"));
✅ Answer:

"push" < "pull" (since 's' > 'l' in ASCII).

Therefore, strcmp() returns a positive value → 1
