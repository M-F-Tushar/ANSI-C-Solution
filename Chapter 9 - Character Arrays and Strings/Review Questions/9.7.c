9.7 Which of the following statements will correctly store the concatenation of strings s1 and s2 in string s3?
Options:
✅ (b) strcat(s1, s2, s3);

strcat(s3, s1) → ✅ Valid

strcat(s3, s2) → ✅ Valid

✅ Correct Answer:
c
Copy
Edit
strcpy(s3, s1);
strcat(s3, s2);

