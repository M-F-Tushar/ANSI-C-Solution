/*
5.14
What happens when an input data item contains
(a) more characters than the specified field width and
(b) fewer characters than the specified field width?
*/
(a) More characters:

Extra characters stay in the buffer.
(b) Fewer characters:

Program waits for more input.
