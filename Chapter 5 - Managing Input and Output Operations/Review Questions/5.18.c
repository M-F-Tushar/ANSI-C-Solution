/*
What happens if an output data item contains
(a) more characters than the specified field width and
(b) fewer characters than the specified field width?
*/
(a) More characters:

No issue, just prints all.
(b) Fewer characters:

Right-padded with spaces.
